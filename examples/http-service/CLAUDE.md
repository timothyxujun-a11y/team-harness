<!-- AUTO-GENERATED BY TEAM HARNESS v2.1.0 -->
<!-- DO NOT EDIT DIRECTLY — run ./scripts/harness render to regenerate -->

# 项目信息

项目名称：http-service-example
项目ct_description }}{% endif %}
Java 版本：17
构建工具：Maven
启用的 Profied_profiles }}{% endif %}

# 核心规则

1. 不得编造不存在的接口、类或配置。
2. 仅修改完成当前任务所需的最小范围。
3. 不得修改生产配置或写入敏感信息。
4. 修改后执行相关编译和测试。
5. 高风险操作必须人工确认。

# 常用命令

```bash
mvn clean compile -DskipTests
mvn test
mvn clean package -DskipTests
```

# 规则索引


- {{ en
- {{ en
- 
{% endfor %}

- 项目本地规则：.harness/local/index.yaml

# 按需加载

执行任务前，根据任务类型和变更文件，使用 `./scripts/harness rules select` 选择相关规则。
禁止默认读取完整规则库。

- 常驻上下文目标：≤ 2000 Token
- 常驻上下文最大文件数：3
- 任务规则预算：≤ 6000 Token / 8 文件
- 代码审查预算：≤ 8000 Token / 15 规则

# 项目本地说明


参见 CLAUDE.local.md
{% else %}
暂无项目本地说明文件。使用 `./scss init` 初始化。
{% endif %}
