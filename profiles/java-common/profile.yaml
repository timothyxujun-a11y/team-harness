apiVersion: harness.company.io/v1
kind: HarnessProfile

metadata:
  name: java-common
  version: 1.0.0
  description: Java 通用编码规范

compatibility:
  languages:
    - java
  javaVersions:
    - "8"
    - "11"
    - "17"
    - "21"
  buildTools:
    - maven

dependsOn: []

conflictsWith: []

content:
  index: index.md
  rules: rules.yaml
  ruleDirectory: rules
