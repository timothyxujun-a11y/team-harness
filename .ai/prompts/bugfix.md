# Prompt 模板 — Bug 修复

## 问题现象

[CUSTOMIZE: 复现步骤、报错信息、预期 vs 实际]

## 输出要求

1. **问题原因**：定位根因到具体代码位置（`文件:行`），解释为什么出错。
2. **修改方案**：最小改动修复，说明改动点。
3. **影响范围**：本次改动可能影响的其他路径。
4. **测试方案**：补充能复现并守住修复的单测；改完跑 `mvn test`。

## 约束

- 只修 Bug，不顺手重构。
- 不确定根因时先提问，不猜测式修改。
